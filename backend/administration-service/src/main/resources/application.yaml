server:
  port: 8083

spring:
  application:
    name: administration-service

  datasource:
    url: jdbc:postgresql://localhost:5432/univapp_administration
    username: postgres
    password: ousseynou.ndoye2@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: univapp-secret-key-2026-very-long-string-for-security
  expiration: 86400000