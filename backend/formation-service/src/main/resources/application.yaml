server:
  port: 8084

spring:
  application:
    name: formation-service

  datasource:
    url: jdbc:postgresql://localhost:5432/univapp_formation
    username: postgres
    password: ousseynou.ndoye2@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: univapp-secret-key-2026-very-long-string-for-security
  expiration: 86400000