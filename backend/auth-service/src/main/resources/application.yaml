server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/univapp_auth
    username: postgres
    password: ousseynou.ndoye2@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          secret: univapp-secret-key-2026-very-long-string-for-security

jwt:
  secret: univapp-secret-key-2026-very-long-string-for-security
  expiration: 86400000